import 'package:flutter/material.dart';

class AllFollowView extends StatelessWidget {
  const AllFollowView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('All Follows')));
  }
}
